// Адрес внутри сайта с учётом базового пути сборки: статическая копия каталога может
// жить не в корне, и все внутренние ссылки и картинки собираются через этот помощник.
export const appHref = (path) => `${import.meta.env.BASE_URL.replace(/\/$/, "")}${path.startsWith("/") ? path : `/${path}`}`;
